{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "rm -rf node_modules && npm install --omit=dev --no-audit --no-fund",
    "useNixpacks": false
  },
  "deploy": {
    "startCommand": "npm start"
  }
}
